name: Frontend Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: self-hosted

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      # Checkout the repository
      - uses: actions/checkout@v4

      # Setup Node.js
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      # Install dependencies (frontend) with legacy-peer-deps to avoid conflicts
      - name: Install frontend dependencies
        run: |
          cd _work/anteso-frontend/anteso-frontend
          rm -rf node_modules package-lock.json
          npm install --legacy-peer-deps

      # Create .env file from secret
      - name: Setup environment
        run: |
          cd _work/anteso-frontend/anteso-frontend
          touch .env
          echo "${{ secrets.ANTESOFE }}" > .env

      # Optional: Build the frontend
      - name: Build frontend
        run: |
          cd _work/anteso-frontend/anteso-frontend
          npm run build

      # Optional: Restart frontend server if applicable
      - name: Restart frontend server
        run: |
          pm2 restart FrontendServer || pm2 start FrontendServer
